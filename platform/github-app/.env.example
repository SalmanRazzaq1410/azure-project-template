# GitHub App Configuration
# Get these from https://github.com/settings/apps

# App ID (from GitHub App settings)
APP_ID=

# Webhook Secret (set when creating the app)
WEBHOOK_SECRET=

# Private Key (download .pem from GitHub App settings, then base64 encode or use path)
# Option 1: Base64 encoded key
PRIVATE_KEY=

# Option 2: Path to .pem file
# PRIVATE_KEY_PATH=./private-key.pem

# Webhook Proxy URL (for local development)
# WEBHOOK_PROXY_URL=https://smee.io/your-channel

# Log level (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info

# Port for the webhook server
PORT=3000
